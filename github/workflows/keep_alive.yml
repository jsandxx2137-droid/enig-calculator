name: Keep Streamlit Alive

on:
  schedule:
    - cron: '*/30 * * * *' # 30분마다 자동 실행
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s -L "https://enig-calculator-vjdpvyntkv3jge3hpfmmzt.streamlit.app" > /dev/null
